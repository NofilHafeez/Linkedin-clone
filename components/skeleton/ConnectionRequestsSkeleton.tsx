'use client';

export default function ConnectionRequests() {
  return (
    <section className="bg-white bg-zinc-900 rounded-xl shadow p-4">
      <h2 className="text-lg text-center font-semibold mb-4">Invitations</h2>
      <div className="space-y-3">
        {/* Example Skeleton Items */}
        <div className="h-16 bg-zinc-700  rounded-lg animate-pulse" />
      </div>
    </section>
  );
}
